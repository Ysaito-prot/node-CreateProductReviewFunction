<!DOCTYPE html>
<html lang="ja">

<head>
  <meta charset="UTF-8">
  <title>User Edit</title>
  <link type="text/css" rel="stylesheet" href="/css/style.css">
</head>

<body>
  <!--formの％は全て消した状態で行うか？-->
  <h1>更新フォーム</h1>
  <form action="/update/<%= user[0].id %>" method="POST">
    <div>
        <label for="username">ユーザー名</label>
        <input type="text" id="username" name="username" value="<%= user[0].username %>">
    </div>
    <div>
        <label for="age">年齢</label>
        <input type="text" id="age" name="age" value="<%= user[0].age %>">
    </div>
    <div>
        <label for="rating">評価</label>
        <input type="text" id="rating" name="rating" value="<%= user[0].rating %>">
    </div>
    <div>
        <label for="reason">評価内容</label>
        <textarea id="reason" name="reason" cols="36" rows="6"><%= user[0].reason %></textarea>
    </div>
    <div class="button"><button class="submitBtn" type="submit">更新</button></div>
  </form>
</body>

</html>
